package main

import (
	"testing"
)

func TestNetworkCreation(t *testing.T) {
	t.Log("Running " + t.Name())
	// 1. Create network with subnet

	// In case of error
	// t.Fatalf("Hello " + t.Name())

}

func TestNetworkRetrieve(t *testing.T) {
	t.Log("Running " + t.Name())
	// 1. Retrieve network with subnet

	// In case of error
	// t.Fatalf("Hello " + t.Name())

}

func TestNetworkDelete(t *testing.T) {
	t.Log("Running " + t.Name())
	// 1. Delete network with subnet

	// In case of error
	// t.Fatalf("Hello " + t.Name())

}

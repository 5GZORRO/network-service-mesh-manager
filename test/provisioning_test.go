package main

import (
	"testing"
)

func TestGatewayConnectivityCreate(t *testing.T) {
	t.Log("Running " + t.Name())
	// 1. Create network with subnet

	// In case of error
	// t.Fatalf("Hello " + t.Name())

}

func TestGatewayConnectivityRetrieve(t *testing.T) {
	t.Log("Running " + t.Name())
	// 1. Retrieve network with subnet

	// In case of error
	// t.Fatalf("Hello " + t.Name())

}

func TestGatewayConnectivityDelete(t *testing.T) {
	t.Log("Running " + t.Name())
	// 1. Delete network with subnet

	// In case of error
	// t.Fatalf("Hello " + t.Name())

}
